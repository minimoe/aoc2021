var solve = require('../11a');
var assert = require("assert");

describe('Day 11', () => {
it('should return correct',
    function() {
        var input = "";
        assert.equal(solve(input), 10);    
    })
});